name: sniperlog
description: SniperLog - Jurnal Trading Scalper

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.1
  sqflite: ^2.3.0
  path: ^1.9.0
  shared_preferences: ^2.3.0
  image_picker: ^1.1.2
  path_provider: ^2.1.3
  share_plus: ^10.1.4
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
